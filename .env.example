# Supabase / Lovable Cloud — client (exposé au navigateur)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Supabase — serveur (server functions / SSR)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_PROJECT_ID=
# Ne PAS committer : service role, bypass RLS
SUPABASE_SERVICE_ROLE_KEY=

# Lovable AI Gateway
LOVABLE_API_KEY=

# Setup initial admin (route /setup)
SETUP_KEY=

# GitHub (connecteur Lovable — publication blog vers rio-uncovered)
GITHUB_API_KEY=

# Endpoint public /api/public/leads — doit être EXACTEMENT la même valeur
# que JEITINHO_HUB_LEADS_SECRET dans l'environnement de jeitinho.fr
# (Cloudflare Workers / Lovable Cloud du site, pas de ce dépôt).
JEITINHO_HUB_LEADS_SECRET=