# Cloudflare Worker runtime bindings are declared in wrangler.jsonc.
# Secrets are configured with Wrangler and are never committed.

# One-shot initial admin bootstrap
SETUP_KEY=

# Optional: GitHub publication from the Worker
GITHUB_API_KEY=

# Optional: password-reset email delivery
RESEND_API_KEY=
RESEND_FROM=
