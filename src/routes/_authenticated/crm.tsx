import { createFileRoute, Link } from "@tanstack/react-router";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { supabase } from "@/integrations/supabase/client";
import { PageShell } from "@/components/page-shell";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { toast } from "sonner";
import { Inbox, Users, FileDown, ExternalLink, Flame, BellRing } from "lucide-react";
import { PriorityBoard } from "@/components/crm/priority-board";
import { FollowupsPanel } from "@/components/crm/followups-panel";

export const Route=createFileRoute("/_authenticated/crm")({component:CrmPage,head:()=>({meta:[{title:"CRM — JEITINHO"}]})});
type LeadStatus="new"|"contacted"|"qualified"|"converted"|"lost"|"spam";
type ProspectStatus="new"|"contacted"|"quoted"|"negotiating"|"won"|"lost";
type Lead={id:string;source:string;status:LeadStatus;name:string;email:string|null;phone:string|null;party_size:number|null;travel_start:string|null;travel_end:string|null;activities:string[];message:string|null;received_at:string;prospect_id:string|null};
type Prospect={id:string;status:ProspectStatus;name:string;email:string|null;phone:string|null;party_size:number|null;travel_start:string|null;travel_end:string|null;client_id:string|null;created_at:string};
const LEAD_STATUS_LABEL:Record<LeadStatus,string>={new:"Nouveau",contacted:"Contacté",qualified:"Qualifié",converted:"Converti",lost:"Perdu",spam:"Spam"};
const PROSPECT_STATUS_LABEL:Record<ProspectStatus,string>={new:"Nouveau",contacted:"Contacté",quoted:"Devis",negotiating:"Négociation",won:"Converti / gagné",lost:"Perdu"};
function fmtRange(start:string|null,end:string|null){if(!start&&!end)return null;if(start&&end&&start!==end)return `${start} → ${end}`;return start??end}
function CrmPage(){return <PageShell eyebrow="Pipeline commercial" title="CRM" description="Les leads du site jeitinho.fr arrivent ici automatiquement — qualifiez-les en prospects, puis en clients."><Tabs defaultValue="priorites"><TabsList><TabsTrigger value="priorites"><Flame className="mr-1.5 h-3.5 w-3.5"/>Priorités</TabsTrigger><TabsTrigger value="relances"><BellRing className="mr-1.5 h-3.5 w-3.5"/>Relances</TabsTrigger><TabsTrigger value="leads"><Inbox className="mr-1.5 h-3.5 w-3.5"/>Leads</TabsTrigger><TabsTrigger value="prospects"><Users className="mr-1.5 h-3.5 w-3.5"/>Prospects</TabsTrigger></TabsList><TabsContent value="priorites" className="mt-6"><PriorityBoard/></TabsContent><TabsContent value="relances" className="mt-6"><FollowupsPanel/></TabsContent><TabsContent value="leads" className="mt-6"><LeadsInbox/></TabsContent><TabsContent value="prospects" className="mt-6"><ProspectsPipeline/></TabsContent></Tabs></PageShell>}
function LeadsInbox(){const qc=useQueryClient();const [filter,setFilter]=useState<"all"|LeadStatus>("all");const {data,isLoading,error}=useQuery({queryKey:["leads"],queryFn:async()=>{const {data,error}=await supabase.from("leads").select("id,source,status,name,email,phone,party_size,travel_start,travel_end,activities,message,received_at,prospect_id").order("received_at",{ascending:false});if(error)throw new Error(error.message);return (data??[]) as Lead[]}});const rows=(data??[]).filter(l=>filter==="all"||l.status===filter);const setStatus=async(id:string,status:LeadStatus)=>{const {error}=await supabase.from("leads").update({status,processed_at:new Date().toISOString()}).eq("id",id);if(error)return toast.error(error.message);qc.invalidateQueries({queryKey:["leads"]})};const qualify=async(lead:Lead)=>{if(lead.prospect_id)return;const {data:prospect,error}=await supabase.from("prospects").insert({name:lead.name,email:lead.email,phone:lead.phone,party_size:lead.party_size,travel_start:lead.travel_start,travel_end:lead.travel_end,activities:lead.activities??[],message:lead.message,source:lead.source,status:"new" as ProspectStatus}).select("id").single();if(error)return toast.error(error.message);const {error:leadErr}=await supabase.from("leads").update({status:"qualified",prospect_id:(prospect as {id:string}).id,processed_at:new Date().toISOString()}).eq("id",lead.id);if(leadErr)return toast.error(leadErr.message);toast.success("Lead qualifié en prospect.");Promise.all([qc.invalidateQueries({queryKey:["leads"]}),qc.invalidateQueries({queryKey:["prospects"]})])};return <div className="space-y-4"><div className="flex flex-wrap gap-2">{[{value:"all" as const,label:"Tous"},...Object.entries(LEAD_STATUS_LABEL).map(([value,label])=>({value:value as LeadStatus,label}))].map(s=><button key={s.value} onClick={()=>setFilter(s.value)} className={`rounded-full border px-3 py-1.5 text-xs ${filter===s.value?"border-primary bg-primary text-primary-foreground":"border-border/60 text-muted-foreground"}`}>{s.label}</button>)}</div>{isLoading?<div className="text-sm text-muted-foreground">Chargement…</div>:error?<Card className="border-destructive/40 p-8"><h3 className="font-semibold">Impossible de charger les leads</h3><p className="mt-2 text-sm text-muted-foreground">{(error as Error).message}</p></Card>:!rows.length?<Card className="border-dashed p-16 text-center"><Inbox className="mx-auto mb-4 h-8 w-8 text-primary"/><h3 className="text-xl">Aucun lead</h3></Card>:<div className="space-y-2">{rows.map(l=><Card key={l.id} className="p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="mb-1 flex items-center gap-2"><span className="pill">{LEAD_STATUS_LABEL[l.status]}</span><span className="text-xs text-muted-foreground">{new Date(l.received_at).toLocaleString("fr-FR")}</span></div><h3 className="font-medium">{l.name}</h3><p className="text-xs text-muted-foreground">{[l.email,l.phone].filter(Boolean).join(" · ")||"Pas de contact"}{l.party_size?` · ${l.party_size} pers.`:""}{fmtRange(l.travel_start,l.travel_end)?` · ${fmtRange(l.travel_start,l.travel_end)}`:""}</p>{l.message&&<p className="mt-2 text-sm text-foreground/80">{l.message}</p>}</div><div className="flex flex-wrap gap-2">{l.status==="new"&&<Button variant="outline" size="sm" onClick={()=>setStatus(l.id,"contacted")}>Marquer contacté</Button>}{!l.prospect_id&&!(["spam","lost"] as string[]).includes(l.status)&&<Button size="sm" className="btn-primary" onClick={()=>qualify(l)}>Qualifier en prospect</Button>}{l.prospect_id&&<span className="self-center text-xs text-muted-foreground">→ déjà un prospect</span>}{l.status!=="spam"&&<Button variant="ghost" size="sm" className="text-destructive" onClick={()=>setStatus(l.id,"spam")}>Spam</Button>}</div></div></Card>)}</div>}</div>}
function ProspectsPipeline(){const qc=useQueryClient();const [filter,setFilter]=useState<"all"|ProspectStatus>("all");const {data,isLoading,error}=useQuery({queryKey:["prospects"],queryFn:async()=>{const {data,error}=await supabase.from("prospects").select("id,status,name,email,phone,party_size,travel_start,travel_end,client_id,created_at").order("created_at",{ascending:false});if(error)throw new Error(error.message);return (data??[]) as Prospect[]}});const rows=(data??[]).filter(p=>filter==="all"||p.status===filter);const convertToClient=async(p:Prospect)=>{const {data:clientId,error}=await supabase.rpc("convert_prospect_to_client",{p_prospect_id:p.id});if(error)return toast.error(error.message);if(!clientId)return toast.error("La conversion n'a pas retourné de client.");toast.success("Prospect converti en client.");Promise.all([qc.invalidateQueries({queryKey:["prospects"]}),qc.invalidateQueries({queryKey:["clients"]})])};return <div className="space-y-4"><div className="flex flex-wrap gap-2">{[{value:"all" as const,label:"Tous"},...Object.entries(PROSPECT_STATUS_LABEL).map(([value,label])=>({value:value as ProspectStatus,label}))].map(s=><button key={s.value} onClick={()=>setFilter(s.value)} className={`rounded-full border px-3 py-1.5 text-xs ${filter===s.value?"border-primary bg-primary text-primary-foreground":"border-border/60 text-muted-foreground"}`}>{s.label}</button>)}</div>{isLoading?<div className="text-sm text-muted-foreground">Chargement…</div>:error?<Card className="border-destructive/40 p-8"><h3 className="font-semibold">Impossible de charger les prospects</h3><p className="mt-2 text-sm text-muted-foreground">{(error as Error).message}</p></Card>:!rows.length?<Card className="border-dashed p-16 text-center"><Users className="mx-auto mb-4 h-8 w-8 text-primary"/><h3 className="text-xl">Aucun prospect</h3></Card>:<div className="space-y-2">{rows.map(p=><Card key={p.id} className="flex flex-wrap items-center justify-between gap-4 p-4"><div><div className="mb-1 flex items-center gap-2"><span className="pill">{PROSPECT_STATUS_LABEL[p.status]}</span><span className="text-xs text-muted-foreground">{new Date(p.created_at).toLocaleDateString("fr-FR")}</span></div><h3 className="font-medium">{p.name}</h3><p className="text-xs text-muted-foreground">{[p.email,p.phone].filter(Boolean).join(" · ")||"Pas de contact"}{p.party_size?` · ${p.party_size} pers.`:""}</p></div><div className="flex flex-wrap gap-2"><Link to="/devis/new" search={{prospectId:p.id}}><Button variant="outline" size="sm"><FileDown className="mr-1.5 h-3.5 w-3.5"/>Créer un devis</Button></Link>{!p.client_id&&p.status!=="lost"&&<Button size="sm" className="btn-primary" onClick={()=>convertToClient(p)}>Convertir en client</Button>}{p.client_id&&<Link to="/clients/$id" params={{id:p.client_id}}><Button variant="ghost" size="sm"><ExternalLink className="mr-1.5 h-3.5 w-3.5"/>Voir le client</Button></Link>}</div></Card>)}</div>}</div>}
